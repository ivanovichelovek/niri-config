*.bak
*.bak2
*.lock
dots/fish/conf.d/secrets.fish

__pycache__/

# The image set lives on the `wallpapers` orphan branch, not here — 195 MB has
# no business in a config repo. install/bootstrap.sh fetches it on demand.
# Ignored so a local working copy of them does not show up as untracked.
/wallpapers/

# Happ subscription database — server credentials, never commit it.
dots/happ/subs.db

# Per-machine overrides — the whole point is that they stay local.
# The .example files are tracked; copy them to create yours.
config.d/99-local.kdl
# Output config (panel scale), generated by bootstrap.sh from the EDID.
config.d/05-local-output.kdl
