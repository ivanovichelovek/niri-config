// ─────────────────────────────────────────────────────────────────────────────
// 70 — Key bindings
// ─────────────────────────────────────────────────────────────────────────────
// Docs: https://niri-wm.github.io/niri/Configuration:-Key-Bindings
//
// "Mod" = Super on bare metal, Alt inside a nested Niri window.
// Use `wev` to discover XKB key names.
// Most actions here can also be triggered via `niri msg action <name>`.
//
// Shell binds call `noctalia msg <command>`. Noctalia cannot grab global keys
// itself (it is a layer-shell client) — the compositor owns every shortcut and
// forwards it over IPC. Run `noctalia msg --help` for the full command list.
// ─────────────────────────────────────────────────────────────────────────────

binds {

    // ═══════════════════════════════════════════════════════════════════════
    // Session & compositor
    // ═══════════════════════════════════════════════════════════════════════

    // Niri's built-in overview (zoomed-out workspace view).
    Mod+Tab repeat=false { toggle-overview; }

    // Quit Niri (shows confirmation dialog).
    Mod+Shift+E { quit; }

    // Escape hatch: re-enable keybinds if an app inhibits them (e.g. RDP client).
    Mod+Escape allow-inhibiting=false { toggle-keyboard-shortcuts-inhibit; }

    // Power off all monitors. Any input wakes them up.
    Mod+Shift+O { power-off-monitors; }

    // Niri's native hotkey cheatsheet — lists every bind in this file.
    // (Was iNiR's own cheatsheet overlay on the same key.)
    Mod+Slash { show-hotkey-overlay; }

    // ═══════════════════════════════════════════════════════════════════════
    // Noctalia shell — panels & tools
    // ═══════════════════════════════════════════════════════════════════════

    // Window switcher overlay.
    // Noctalia's `window-switcher` takes only [close] — there is no "previous"
    // direction, so Alt+Shift+Tab has no equivalent. Niri's own focus binds
    // (Mod+H/J/K/L, Mod+arrows) cover reverse navigation.
    Alt+Tab { spawn "noctalia" "msg" "window-switcher"; }
    // Alt+Shift+Tab — no counterpart in noctalia msg.

    // App launcher.
    Mod+Space repeat=false { spawn "noctalia" "msg" "panel-toggle" "launcher"; }

    // Clipboard history panel (reads Noctalia's own store, not cliphist).
    Mod+V { spawn "noctalia" "msg" "panel-toggle" "clipboard"; }

    // Control center (network, bluetooth, audio, power profile…).
    // New — iNiR had no single equivalent. Drop it if the key is wanted elsewhere.
    Mod+Shift+C { spawn "noctalia" "msg" "panel-toggle" "control-center"; }

    // Lock screen.
    Mod+Alt+L allow-when-locked=true { spawn "noctalia" "msg" "session" "lock"; }

    // Lock, then suspend (wrapper script also restarts wlsunset on resume).
    Mod+Alt+Shift+L allow-when-locked=true { spawn "/home/ivanc/.local/bin/lock-and-suspend"; }

    // Region screenshot.
    Mod+Shift+S { spawn "noctalia" "msg" "screenshot-region"; }

    // Fullscreen screenshot of the focused monitor.
    // New — closest thing to iNiR's region tool family.
    Mod+Ctrl+S { spawn "noctalia" "msg" "screenshot-fullscreen"; }

    // No noctalia counterpart — these were iNiR region-tool modes:
    //   Mod+Shift+X  OCR on a selected region
    //                (see AUR: noctalia-plugin-screenshot-ocr)
    //   Mod+Shift+A  Google Lens search on a selected region
    //   Mod+Shift+R  screen recording with sound
    //                (bind wf-recorder or a script here if you want it back)

    // Wallpaper picker.
    Ctrl+Alt+T { spawn "noctalia" "msg" "panel-toggle" "wallpaper"; }

    // Shell settings window.
    Mod+Comma { spawn "noctalia" "msg" "settings-toggle"; }

    // Session / power dialog.
    Mod+Shift+Q { spawn "noctalia" "msg" "panel-toggle" "session"; }

    // No noctalia counterpart — iNiR-specific:
    //   Super+G      crosshair overlay toggle
    //   Mod+Shift+W  cycle iNiR panel families ("ii" / "waffle")

    // ═══════════════════════════════════════════════════════════════════════
    // App launchers
    // ═══════════════════════════════════════════════════════════════════════
    // Previously routed through `inir terminal` / `inir browser`, which read
    // apps.terminal ("kitty -1") and apps.browser ("zen-browser") from iNiR's
    // config.json. Noctalia has no such indirection — spawn them directly.

    Mod+T { spawn "kitty" "-1"; }
    Mod+Return { spawn "kitty" "-1"; }
    Super+E { spawn "nautilus"; }
    Super+W { spawn "zen-browser"; }

    // ═══════════════════════════════════════════════════════════════════════
    // Window management
    // ═══════════════════════════════════════════════════════════════════════

    // Close window. Niri-native — iNiR wrapped this in its own confirmation
    // dialog for unsaved work; noctalia has no equivalent, so this closes at once.
    Mod+Q repeat=false { close-window; }

    // Maximize column (fills screen width, keeps gaps/bar).
    Mod+D { maximize-column; }

    // Maximize window to screen edges (no gaps, like traditional maximize).
    // Mod+M { maximize-window-to-edges; }

    // Fullscreen (app goes borderless edge-to-edge).
    Mod+F { fullscreen-window; }

    // Toggle floating / tiling.
    Mod+A { toggle-window-floating; }

    // Switch focus between tiling and floating layers.
    Mod+Shift+V { switch-focus-between-floating-and-tiling; }

    // ═══════════════════════════════════════════════════════════════════════
    // Column layout (Niri's scrolling-tiler model)
    // ═══════════════════════════════════════════════════════════════════════

    // Cycle through preset column widths (1/3 → 1/2 → 2/3, set in layout).
    Mod+R { switch-preset-column-width; }

    // Reset window height to default.
    Mod+Ctrl+R { reset-window-height; }

    // Center the focused column on screen.
    Mod+C { center-column; }

    // Center all fully visible columns.
    // Mod+Ctrl+C { center-visible-columns; }

    // Expand column to fill space not used by other visible columns.
    // Mod+Ctrl+F { expand-column-to-available-width; }

    // Fine-grained width/height adjustment (±10% of screen).
    Mod+Minus { set-column-width "-10%"; }
    Mod+Equal { set-column-width "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }

    // Consume the window to the right into the focused column (stack vertically),
    // or expel the bottom window from the column to its own column.
    Mod+BracketLeft  { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }

    // Toggle tabbed display for the focused column.
    // Windows show as vertical tabs instead of stacked.
    // NOTE: Mod+W conflicts with Super+W (browser). Uncomment with a different key if desired.
    // Mod+W { toggle-column-tabbed-display; }

    // ═══════════════════════════════════════════════════════════════════════
    // Focus navigation (arrows + Vim keys)
    // ═══════════════════════════════════════════════════════════════════════

    Mod+Left  { focus-column-left; }
    Mod+Right { focus-column-right; }
    Mod+Up    { focus-window-up; }
    Mod+Down  { focus-window-down; }
    Mod+H     { focus-column-left; }
    Mod+J     { focus-window-down; }
    Mod+K     { focus-window-up; }
    Mod+L     { focus-column-right; }

    // Jump to first / last column.
    Mod+Home { focus-column-first; }
    Mod+End  { focus-column-last; }

    // ═══════════════════════════════════════════════════════════════════════
    // Move windows (arrows + Vim keys)
    // ═══════════════════════════════════════════════════════════════════════

    Mod+Shift+Left  { move-column-left; }
    Mod+Shift+Right { move-column-right; }
    Mod+Shift+Up    { move-window-up; }
    Mod+Shift+Down  { move-window-down; }
    Mod+Shift+H     { move-column-left; }
    Mod+Shift+J     { move-window-down; }
    Mod+Shift+K     { move-window-up; }
    Mod+Shift+L     { move-column-right; }

    // Move column to first / last position.
    Mod+Ctrl+Home { move-column-to-first; }
    Mod+Ctrl+End  { move-column-to-last; }

    // ═══════════════════════════════════════════════════════════════════════
    // Multi-monitor
    // ═══════════════════════════════════════════════════════════════════════
    // Focus the monitor in the given direction.
    Mod+Ctrl+Left  { focus-monitor-left; }
    Mod+Ctrl+Right { focus-monitor-right; }
    Mod+Ctrl+Up    { focus-monitor-up; }
    Mod+Ctrl+Down  { focus-monitor-down; }

    // Move the focused column to the monitor in the given direction.
    Mod+Ctrl+Shift+Left  { move-column-to-monitor-left; }
    Mod+Ctrl+Shift+Right { move-column-to-monitor-right; }
    Mod+Ctrl+Shift+Up    { move-column-to-monitor-up; }
    Mod+Ctrl+Shift+Down  { move-column-to-monitor-down; }

    // Move entire workspace to another monitor:
    // Mod+Ctrl+Shift+Left { move-workspace-to-monitor-left; }

    // ═══════════════════════════════════════════════════════════════════════
    // Workspaces
    // ═══════════════════════════════════════════════════════════════════════
    // Named workspaces (term, web, chat, code, music, vpn) and the per-app
    // assignment rules live in 90-user-extra.kdl. Index 1..6 matches that order.

    // Focus workspace by index.
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }

    // Move focused column to workspace by index.
    Mod+Ctrl+1 { move-column-to-workspace 1; }
    Mod+Ctrl+2 { move-column-to-workspace 2; }
    Mod+Ctrl+3 { move-column-to-workspace 3; }
    Mod+Ctrl+4 { move-column-to-workspace 4; }
    Mod+Ctrl+5 { move-column-to-workspace 5; }
    Mod+Ctrl+6 { move-column-to-workspace 6; }
    Mod+Ctrl+7 { move-column-to-workspace 7; }
    Mod+Ctrl+8 { move-column-to-workspace 8; }
    Mod+Ctrl+9 { move-column-to-workspace 9; }

    // Navigate workspaces sequentially (useful with dynamic workspaces).
    Mod+Page_Down { focus-workspace-down; }
    Mod+Page_Up   { focus-workspace-up; }
    Mod+Ctrl+Page_Down { move-column-to-workspace-down; }
    Mod+Ctrl+Page_Up   { move-column-to-workspace-up; }

    // Reorder workspaces.
    // Mod+Shift+Page_Down { move-workspace-down; }
    // Mod+Shift+Page_Up   { move-workspace-up; }

    // Mouse wheel workspace navigation (rate-limited to avoid flying through).
    Mod+WheelScrollDown      cooldown-ms=150 { focus-workspace-down; }
    Mod+WheelScrollUp        cooldown-ms=150 { focus-workspace-up; }
    Mod+Ctrl+WheelScrollDown cooldown-ms=150 { move-column-to-workspace-down; }
    Mod+Ctrl+WheelScrollUp   cooldown-ms=150 { move-column-to-workspace-up; }

    // Mouse wheel column navigation.
    Mod+WheelScrollRight { focus-column-right; }
    Mod+WheelScrollLeft  { focus-column-left; }

    // ═══════════════════════════════════════════════════════════════════════
    // Screenshots (Niri native — Noctalia's own tools are on Mod+Shift+S above)
    // ═══════════════════════════════════════════════════════════════════════

    Print      { screenshot; }
    Ctrl+Print { screenshot-screen; }
    Alt+Print  { screenshot-window; }

    // ═══════════════════════════════════════════════════════════════════════
    // Hardware keys → Noctalia IPC (with OSD feedback)
    // ═══════════════════════════════════════════════════════════════════════

    // Volume. Routed through the shell so the OSD appears and the right sink
    // is resolved; `noctalia msg volume-up/down [step]` takes an optional step.
    XF86AudioRaiseVolume allow-when-locked=true { spawn "noctalia" "msg" "volume-up"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn "noctalia" "msg" "volume-down"; }
    XF86AudioMute        allow-when-locked=true { spawn "noctalia" "msg" "volume-mute"; }
    XF86AudioMicMute     allow-when-locked=true { spawn "noctalia" "msg" "mic-mute"; }

    // Brightness.
    XF86MonBrightnessUp   allow-when-locked=true { spawn "noctalia" "msg" "brightness-up"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "noctalia" "msg" "brightness-down"; }

    // Media playback (MPRIS — works with any player: Spotify, Firefox, mpv…).
    XF86AudioPlay  { spawn "noctalia" "msg" "media" "toggle"; }
    XF86AudioPause { spawn "noctalia" "msg" "media" "toggle"; }
    XF86AudioNext  { spawn "noctalia" "msg" "media" "next"; }
    XF86AudioPrev  { spawn "noctalia" "msg" "media" "previous"; }

    // Keyboard shortcuts for media (when no media keys are available).
    Ctrl+Mod+Space { spawn "noctalia" "msg" "media" "toggle"; }
    Mod+Alt+N      { spawn "noctalia" "msg" "media" "next"; }
    Mod+Alt+P      { spawn "noctalia" "msg" "media" "previous"; }
    Mod+Shift+M    { spawn "noctalia" "msg" "volume-mute"; }
    Mod+Shift+P    { spawn "noctalia" "msg" "media" "toggle"; }
    Mod+Shift+N    { spawn "noctalia" "msg" "media" "next"; }
    Mod+Shift+B    { spawn "noctalia" "msg" "media" "previous"; }
}
